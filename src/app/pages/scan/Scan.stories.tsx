import React from 'react';
import Scan from './Scan';

export default {
  component: Scan,
  title: 'Pages/Scan',
  parameters: {
    layout: 'fullscreen',
  },
};

export const Default = () => <Scan />;
